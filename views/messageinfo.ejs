<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Info</title>
    <style>
        body {
            font-family: system-ui, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 40px;
            background-color: #f9f9f9;
            color: #333;
        }

        div {
            margin: 12px 0;
            font-size: 1rem;
            text-align: center;
        }

        button {
            padding: 8px 16px;
            font-size: 1rem;
            background-color: #222;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background 0.2s ease;
        }

        button:hover {
            background-color: #444;
        }

        a {
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div>
        MESSAGE INFO:
    </div>
    <div>
        <%=message %>
    </div>
    <div>
        <a href="/<%=username %>/<%=password %>/<%=userid %>">
            <button>
                Return Back
            </button>
        </a>
    </div>
</body>

</html>